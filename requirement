Installation

To run this project locally:

Clone the repository.
Install the required dependencies using:
pip install -r requirements.txt
Required Libraries

The project uses the following Python libraries:

pandas
numpy
scikit-learn
nltk
matplotlib
joblib

Create a requirements.txt file containing these libraries so that anyone cloning the repository can install all dependencies with a single command.
